/**
 * Page gutter + max width — same as `Footer` inner wrapper so header, main, and footer align.
 */
export const appShellContainerClassName = "w-full max-w-7xl mx-auto px-4";

/** Vertical rhythm between homepage marketing sections (bottom padding only). */
export const homeSectionSpacingClassName =
  "pb-8 sm:pb-12 md:pb-16 lg:pb-20";

/** Top padding for the first section after the hero/stats block. */
export const homeSectionTopSpacingClassName =
  "pt-8 sm:pt-12 md:pt-16 lg:pt-20";
